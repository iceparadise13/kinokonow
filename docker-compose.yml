version: "3.3"

services:
  listener:
    image: k8tems/kinokonow_listener
    environment:
      - PYTHONUNBUFFERED=0
    entrypoint:
      ls -ls
    configs:
      - yahoo_api_key

configs:
  yahoo_api_key:
    file: secrets/yahoo_api_key
