version: "3.3"

services:
  listener:
    image: k8tems/kinokonow_listener
    environment:
      - PYTHONUNBUFFERED=0
    entrypoint:
      sleep 1000000
    secrets:
      - yahoo_api_key

  mongo:
    image: mongo

  redis:
    image: redis

secrets:
  yahoo_api_key:
    external: true
